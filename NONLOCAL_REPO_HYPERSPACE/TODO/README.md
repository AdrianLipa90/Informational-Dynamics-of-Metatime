# TODO

Pending tasks for the nonlocal hyperspace layer:
- derive graph automatically from manifests and raw import graph
- connect README mesh upward / sideways / downward
- assign real orbital weights
- compute coherence-sensitive centrality
- bind to dashboard layer when UI appears
