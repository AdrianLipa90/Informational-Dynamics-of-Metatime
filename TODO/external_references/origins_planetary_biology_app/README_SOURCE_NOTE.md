# Source note

This intake preserves the uploaded `origins_planetary_biology_app` as a clean external mini-repo.
It remains outside the canonical runtime until its interfaces, assumptions, and operator bindings
are reconciled against the current CIEL Foundations and Omega sectors.
